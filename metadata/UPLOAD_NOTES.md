# Upload Notes

Status: READY-TO-UPLOAD/PUBLIC review.

The package contains authored notebooks/Python scripts, CSV inputs/results, GIS layers, compressed large shapefile bundles, and a publisher-final paper reference. It does not contain personal or controlled-access human-subject data. Before upload, add the repository-level license chosen for authored code/data and confirm whether the 131 MB GTFS source zip and the compressed GIS zips should be stored through Git LFS or release assets rather than ordinary Git blobs.

The exact OTP batch-query wrapper is not an active request. The paper documents OpenTripPlanner as the external runtime; the reproducibility value here is in the packaged GTFS generation workflow, inputs, scenario GIS layers, and access/frequency outputs.
